#include "worker.h"

int main(int argc, char **argv)
{
    // Do appropriate works among many workcases
    // Details in Worker() function.
    GCoM::Worker t;
    
    return t.DoWork(argc, argv);
}